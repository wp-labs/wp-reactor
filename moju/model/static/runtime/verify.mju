// ---------------------------------------------------------------------------
// runtime domain — flow verification cases
// ---------------------------------------------------------------------------

verify ReactorStarts for flow ReactorStart {
  given {
    RunCommand.work_dir = "/tmp/wp-reactor"
  }

  when RunCommand

  expect {
    Reactor.base_dir == "/tmp/wp-reactor"
  }
}
