// ---------------------------------------------------------------------------
// engine domain — flows
// Crate: wf-engine
// ---------------------------------------------------------------------------
// （无代码背书的数据入口消息 IncomingBatch / MemoryPressure / NewEvents 已随模型瘦身
//  删除，ProcessEvents / EvictWindow / StatsWindowClose 概念流一并移除 —— 事件经
//  runtime Receiver 以 Arrow IPC 流入 WindowManager，属代码 API 级调用，不在模型内
//  建模；告警产出以 AlertOutput 模块类型为准。）
